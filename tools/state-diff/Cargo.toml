[workspace]

[package]
name = "state-diff"
version = "0.1.0"
edition = "2024"
publish = false
description = "MDBX state-root diff tool — compare two chain.db dirs head-to-head to investigate divergence at halt time"

[dependencies]
sentrix-storage = { path = "../../crates/sentrix-storage" }
sentrix-primitives = { path = "../../crates/sentrix-primitives" }
bincode = "1.3"
hex = "0.4"
anyhow = "1"
