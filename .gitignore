# ============================================================
# Sentrix Chain — strict .gitignore
# Backed up by .githooks/pre-commit which scans staged content.
# ============================================================

# ── Build artifacts ─────────────────────────────────────────
/target/
/tools/*/target/
*.rlib
*.rmeta

# ── Chain data & DBs (NEVER commit) ─────────────────────────
/data/
*.db
*.db-*
/test-ledger/

# ── Wallets, keys, secrets (NEVER commit) ───────────────────
# Keystore files (encrypted, but still sensitive)
*.keystore
*.keystore.json

# Raw key material
*.key
*.pem
*.p12
*.pfx
*.enc
*.credentials

# Wallet JSON (broad — any file with "wallet" in name)
*wallet*.json
**/wallets/
**/genesis_wallets*
**/node_keys/

# Anything literally named "private" or "secret" (no extension restriction)
*private*
*secret*
*.secret

# Env files (allow .env.example only)
.env
.env.*
*.env
*.env.*
!.env.example

# ── Build helper / dev-only scripts ─────────────────────────
*.bat
blog.txt
tlog.txt

# ── Operator-specific deploy scripts (operator-internal repo) ─
# These bake in operator infrastructure (wg1 IPs via fleet.env, role
# mapping, satya_master SSH key path). Keeping them out of the public
# repo avoids leaking operational topology and recurring depersonalization
# headaches. Third-party validators should use scripts/deploy-validator.sh
# (the generic primitive) and write their own orchestrator.
scripts/fast-deploy.sh
scripts/emergency-deploy.sh

# ── OS files ─────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── IDE ──────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── Backup, temp, logs ───────────────────────────────────────
*.bak
*.backup
*.old
*.tmp
tmp/
*.log
log-*.txt
logs/

# ── Docker runtime ───────────────────────────────────────────
docker-compose.override.yml
*.tar
*.tar.gz

# ── Crash dumps (root-level only — avoids matching src/core/) ─
/core
/core.*
*.dmp

# ── Duplicate env template (keep .env.example only) ─────────
env.example

# ── Python bytecode (benchmark/ scripts) ────────────────────
__pycache__/
*.pyc
*.pyo

# ── Rust editor backup files ────────────────────────────────
**/*.rs.bk

# ── Local tool / editor workspace dirs ──────────────────────
# Bracket-expression patterns avoid naming third-party tools in public repo.
.cl[a-z]ude/
.cur[a-z]or/
